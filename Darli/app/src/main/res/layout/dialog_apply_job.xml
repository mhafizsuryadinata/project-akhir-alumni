<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@color/bg_card">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Surat Lamaran (Opsi)"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="@color/text_heading"
        android:layout_marginBottom="12dp"/>

    <EditText
        android:id="@+id/etCoverLetter"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:background="@drawable/bg_input_account"
        android:gravity="top"
        android:hint="Tulis pesan singkat untuk perusahaan..."
        android:textColorHint="@color/text_hint_muted"
        android:textColor="@color/text_heading"
        android:padding="16dp"
        android:textSize="14sp"
        android:layout_marginBottom="24dp"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Upload CV (PDF/DOC)"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="@color/text_heading"
        android:layout_marginBottom="12dp"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSelectCV"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="Pilih File CV"
        app:cornerRadius="12dp"
        app:strokeColor="@color/web_primary"
        android:textColor="@color/web_primary"
        app:icon="@drawable/ic_download"
        app:iconTint="@color/web_primary"
        app:iconGravity="textStart"/>

    <TextView
        android:id="@+id/tvSelectedFile"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="No file selected"
        android:textSize="12sp"
        android:textColor="@color/status_info"
        android:layout_marginTop="8dp"
        android:visibility="gone"/>

</LinearLayout>
